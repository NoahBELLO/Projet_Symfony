<div class="p-4 md:w-1/3 sm:mb-0 mb-6">
	<h2 class="text-xl font-semibold title-font text-teal-300 mt-5">{{ job.title }}</h2>
	<p class="text-base leading-relaxed mt-2 text-teal-100">{{ job.description|slice(0, 100) }}...</p>
	<a href="{{ path('app_job_show', {'id': job.id}) }}" class="inline-flex items-center mt-3 text-teal-400 hover:text-white transition-colors">
		En savoir plus
		<svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-4 h-4 ml-2" viewbox="0 0 24 24">
			<path d="M5 12h14M12 5l7 7-7 7"></path>
		</svg>
	</a>
</div>